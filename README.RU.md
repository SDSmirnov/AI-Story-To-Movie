# AI-Story-to-Movie (v1beta)

**AI-Story-to-Movie** — это комплексный автоматизированный пайплайн на Python для преобразования текстовых историй (книг, сценариев) в кинематографические видеоролики. Проект использует передовые модели Google Gemini, Imagen 3 и Veo для создания сценария, генерации персонажей, отрисовки кадров и их анимации.

```bash
INPUT(Story.TXT) | OUTPUT(Movie.MP4)
```

## ACHTUNG / DISCLAIMER / ОТМАЗКИ

⚠️ ОТМАЗКА #1: Это экспериментальный пайплайн, а не волшебная красная кнопка «Сделай мне оскароносный фильм за чашкой кофе». Будьте готовы к ручным правкам, перегенерациям и неожиданным тратам на API.

⚠️ ОТМАЗКА #2: Проект работает, но требует времени, терпения и денег на квоты Veo/Gemini. Если хотите кино «по щелчку» — это не сюда, идите в Netflix.

⚠️ ОТМАЗКА #3: Если у вас проект не заработает, то "Пилите, Шура, пилите".

⚠️ ОТМАЗКА #4: Project Status: Proof of Concept. Not maintained. API compatibility not guaranteed.


## 🎬 Демонстрация результата
Посмотрите пример фильма, полностью сгенерированного этим пайплайном:

[![AI Film: Exhalation)](https://img.youtube.com/vi/l7-UIGjO43Q/0.jpg)](https://www.youtube.com/watch?v=l7-UIGjO43Q)

---

## 🚀 Основные возможности

* **Анализ стиля (Style Master):** Автоматически определяет жанр, атмосферу и визуальный стиль на основе текста произведения.
* **Авто-кастинг (Auto-Casting):** Выявляет ключевых персонажей, создает их детальное описание и генерирует референсные изображения для сохранения консистентности (постоянства) лиц в разных сценах.
* **Кинематографическая раскадровка:** Разбивает текст на сцены и панели, генерируя пары кадров `Start` (начало действия) и `End` (результат) для плавной анимации.
* **Генерация видео (Google Veo):** Превращает статические пары кадров в 4-8 секундные видеоклипы с высокой временной согласованностью.
* **Умное озвучивание:** Создает аудиодорожку с распределением ролей (рассказчик, персонажи) и генерацией SFX (звуковых эффектов) через ElevenLabs.
* **Гибкая настройка стилей:** Поддержка пресетов: Реализм, Аниме, Комикс, Графическая новелла и др..

---

## 🏗 Архитектура проекта

Процесс разделен на 5 последовательных этапов:

1. 🎩 **Style Master (`00_style_master.py`)**: Анализ текста и генерация системных промптов под выбранный стиль.
2. 🎬 **Cinematic Preroll (`01_cinematic_preroll.py`)**: "Режиссерский" этап — кастинг, генерация ключевых кадров (Keyframes) и метаданных сцен.
3. 📹 **Image Animator (`02_image_animator.py`)**: "Операторский" этап — анимация кадров с помощью модели Veo.
4. 🎙️ **Sound Producer (`03_sound_producer.py`)**: "Звукорежиссерский" этап — генерация речи (TTS) и звуков (SFX), создание монтажного листа (EDL).
5. 🎛️ **Audio Assembler (`04_audio_assembler.py`)**: Сведение финального аудиофайла.

---

## 🛠 Требования

* **Python 3.10+**
* **FFmpeg** (установлен и доступен в PATH).
* API ключи:
    * `IMG_AI_API_KEY` / `GOOGLE_API_KEY` (Google Gemini/Veo).
    * `ELEVEN_API_KEY` (Опционально, для SFX).

### Установка зависимостей

```bash
pip install google-generativeai pillow pydub moviepy elevenlabs

```

---

## 📖 Использование

### 1. Генерация стилистических промптов

Анализирует текст книги и создает конфигурацию (`style.md`, `casting.md` и др.) в папке `custom_prompts`.

```bash
python src/00_style_master.py path/to/story.txt --style realistic_movie

```

*Доступные стили:* `realistic_movie`, `anime`, `comic_book`, `graphic_novel`, `watchmen_style`.

### 2. Создание сцен и кадров (Преролл)

Генерирует референсы персонажей и изображения для всех сцен.
Использует флаг `--custom-prompts` для применения настроек из этапа 1.

```bash
python src/01_cinematic_preroll.py path/to/story.txt --custom-prompts

```

*Результат:* Изображения в `cinematic_render/panels` и метаданные в `animation_metadata.json`.

### 3. Анимация изображений

Превращает пары изображений (Start/End) в видеоклипы с помощью Google Veo.

```bash
python src/02_image_animator.py

```

*Результат:* MP4 файлы в `cinematic_render/clips`.

### 4. Генерация звука

Создает реплики персонажей и звуковые эффекты на основе сценария.

```bash
python src/03_sound_producer.py path/to/story.txt

```

*Результат:* Аудиофайлы и EDL JSON в `cinematic_render/audio_master`.

### 5. Сборка аудио

Сводит все звуки в единый файл на основе EDL (Edit Decision List).

```bash
python src/04_audio_assembler.py cinematic_render/audio_master/scene_001_audio_EDL.json final_audio.mp3

```

*Результат:* Готовый аудиофайл `final_audio.mp3`.

---

## ⚙️ Конфигурация

Вы можете вручную настраивать параметры генерации в файле `src/custom_prompts/config.json`:

```json
{
  "format": {
    "type": "single_grid_animation",
    "panels_per_scene": 9
  },
  "image_generation": {
    "resolution": "2K",
    "aspect_ratio": "16:9"
  },
  "animation": {
    "enabled": true,
    "keyframe_type": "start_end"
  }
}

```

---

## 📂 Структура проекта

```text
.
├── demo/                   # Примеры данных (Айзек Азимов "Reason")
├── src/
│   ├── 00_style_master.py      # Анализатор стиля
│   ├── 01_cinematic_preroll.py # Генератор кадров
│   ├── 02_image_animator.py    # Аниматор (Veo)
│   ├── 03_sound_producer.py    # Генератор звука
│   ├── 04_audio_assembler.py   # Сборщик звука
│   ├── custom_prompts/         # Сгенерированные промпты
│   ├── prompts/                # Шаблоны промптов
│   └── tools/                  # Утилиты (ffmpeg wrapper, smart-dubbing)
└── cinematic_render/       # Папка вывода (создается автоматически)
    ├── panels/             # Сгенерированные изображения
    ├── clips/              # Готовые видеофрагменты
    ├── audio_master/       # Аудио и EDL файлы
    └── animation_metadata.json # Главный файл сценария

```

---

## ⚠️ Важные замечания

1. **API Quotas:** Генерация видео через Veo и изображений через Imagen потребляет квоты API. Скрипт обрабатывает ошибку 429 (Resource Exhausted) и предлагает продолжить позже.


2. **Протокол качества:** В системе заложен "Протокол самокритики" (Shit, redo protocol), заставляющий модель перепроверять свои ответы для улучшения качества сценариев.


3. **Ref Dir:** Папка `ref_thriller` (или аналогичная) используется для хранения референсов персонажей. Если персонаж уже есть там, новый генерироваться не будет.


---

## 💡 Know-how's & How-to's (Хитрости и Рекомендации)

В основе пайплайна лежит несколько нетривиальных инженерных и промпт-инженерных решений. Понимание этих механизмов позволит вам получать результат уровня "кино", а не просто слайд-шоу.

### 1. Протокол "Говно, переделывай" (Self-Correction Protocol)

В `01_cinematic_preroll.py`  используется агрессивный системный промпт для LLM. Мы не просто просим модель разбить текст на сцены, а заставляем её пройти 4 итерации самокритики перед выдачей JSON:

> *"1. Пойми, почему твой ответ — говно... 2. Переделай... 3. Еще раз проверь..."*

**Совет:** Если сцены получаются слишком простыми, не меняйте код — просто увеличьте "градус" критики в переменной `SYSTEM_E` внутри скрипта.

### 2. Консистентность персонажей (Auto-Casting Hack)

Самая большая проблема AI-видео — персонажи меняют лица от сцены к сцене.

* **Как это работает:** Скрипт сначала генерирует "паспортные фото" героев и сохраняет их в папку `ref_thriller`. При генерации сцен эти фото скармливаются модели (Imagen/Gemini) как референсы.


* **Хитрость:** Вы можете **вручную заменить файлы** в папке `ref_thriller` на свои (например, фото реальных актеров или более удачные генерации Midjourney). Главное — сохранить имена файлов. Пайплайн автоматически подхватит ваши референсы для всех будущих сцен.

### 3. Метод Dual-Grid для Veo

Модель Google Veo (как и Runway/Luma) лучше работает, когда понимает начало и конец движения.

* **Логика:** Мы генерируем **одно** изображение 4K, в котором вертикально состыкованы два кадра: Сверху — Start Frame, Снизу — End Frame.

* **Зачем:** Это гарантирует, что освещение, декорации и одежда героя будут идентичны в начале и конце клипа. Модели остается только интерполировать движение, минимизируя галлюцинации фона.

### 4. "Актерская игра" в TTS (Hidden Instructions)

Обычный TTS читает текст монотонно. Мы используем Gemini TTS с "режиссерским промптом".

* **Механика:** Мы скармливаем модели текст вида: `[Шепотом, с ужасом]: Оно удержало луч!`. Системный промпт запрещает модели читать текст в скобках вслух, но заставляет применить эту эмоцию к генерации аудио.
* **Совет:** В файле `smart-dubbing` можно вручную поправить JSON с планом озвучки (`dubbing_plan.json`), добавив свои эмоциональные ремарки перед генерацией аудио.

### 5. Экономия токенов и денег

Видео-генерация — дорогое удовольствие.

* **Smart References:** Скрипт `02_image_animator.py` анализирует сцену перед отправкой в Veo. Если персонаж стоит спиной или далеко, скрипт **не отправляет** референс лица в API, экономя дорогие токены контекста.


* **Кэширование:** Все скрипты проверяют наличие выходных файлов. Если клип `clip_001.mp4` уже существует и не пуст, генерация пропускается. Чтобы переделать сцену, просто удалите конкретный файл.

### 6. Ручное вмешательство (Human-in-the-loop)

Пайплайн спроектирован как модульный конструктор. Вы можете остановить процесс на любом этапе:

1. **После преролла:** Откройте `cinematic_render/panels`, удалите неудачные кадры или перерисуйте их в Photoshop/Inpainting.
2. **Перед анимацией:** Поправьте `animation_metadata.json`, изменив промпты движения (`motion_prompt`), если Veo не понимает задачу.
3. **Перед сведением:** Замените отдельные аудиофайлы в `audio_master` на свои записи голоса, сохранив имена файлов. Сборщик склеит всё идеально.

### 7. Принцип "JUST BEFORE ACTION"
При генерации промптов для панелей (изображений) критически важно описывать не само действие, а **мгновение до него**.
* *Плохо:* "Робот бьет кулаком по столу". (Статичная картинка будет смазана или нелогична).
* *Хорошо:* "Робот занес кулак над столом в порыве ярости, за мгновение до удара".
Это дает модели видеогенерации (Veo) "потенциальную энергию", которую она естественным образом преобразует в движение.

---

## 📊 Case Study: "Логика" (Айзек Азимов)

*Реальные метрики создания 14-минутного фильма на базе версии v1beta.*

**Тайминг и затраты:**
* **Итоговый хронометраж:** 14 минут.
* **Время производства:** 3 вечера (один человек).
* **Объем работы:** 19 сцен (формат 3×3), 171 ключевой кадр.

**Статистика генераций:**

* **Анимация (Google Veo API):** ~75 шотов.
    * *Veo 3.1 Fast:* 70 шотов (основная масса).
    * *Veo 3.1 (Full):* 5 шотов (сложные сцены с отсутствующими в кадре персонажами).
    * *Сбои (Moderation/Fail):* ~7% (5 из 70).

* **Ручная доработка (External Tools):** ~96 шотов (Grok Imagine/Runway чтобы не платить за Veo API).

* **Правки (Refinement):**
    * *Перегенерация сцен:* 5 из 19 (баги кастинга, "левые" люди в локациях).
    * *Правки кадров:* ~10% (16 из 171) — уточнение лиц и деталей.

**Пайплайн звука:**

Whisper (распознавание таймингов оригинала) → Gemini Translation (перевод) → Gemini TTS (озвучка) → Shotcut (сведение). Результат требует минимальных правок на монтаже.

## 🎬 Полный производственный стек (Production Stack)

*На примере создания фильма "Reason" (А. Азимов)*

* 🐍 **Ядро (Core):** Python (`AI-Story-to-Movie` pipeline).
* 🧠 **Сценарий и Кастинг:** Gemini 2.5 Pro.
* 🎨 **Раскадровка (Storyboard):** Imagen 3 / Nano Banana Pro.
* 🎥 **Анимация (VFX):** Google Veo 3.1 (API) + Grok Imagine (Refinements).
* 🗣️ **Озвучка (Voices):** Gemini 2.5 Flash TTS.
* 🔊 **Звуковые эффекты (SFX):** ElevenLabs.
* 🎵 **Музыка (OST):** Suno AI.
* ✂️ **Пост-продакшн:** Shotcut + FFmpeg.


---

## TODO

Нужны будут ещё ИИ-агенты - критики:

1. Критика разбивки на сцены - "а полностью ли этот набор сцен раскрывает историю? а не слишком ли мало/много времени здесь ушло?" - разбить/укрупнить
2. Критика раскадровки - "а нет ли здесь критичных галлюцинаций?" - перерисовать панель
3. Критика анимации - "а не сглючила ли здесь модель? а может это нужно без цензуры в sendframe?" - перегенерировать анимацию
4. Критика сведения звука - "а вовремя ли здесь спецэффект? а не говорит ли рот раньше/позже? текста много/мало?" - переписать диалоги для озвучки
Для POC/MVP роль критика пока у человека, но архитектура пайплайна позволяет внедрить роли критиков. Да, это будет медленнее и дороже, но и результат будет лучше. Но это не точно 😉

---

**Лицензия:** WTFPL
**(c) 2026, E.Z. AI-Story-to-Movie Project**

